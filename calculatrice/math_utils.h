#ifndef MATH_UTILS_H
// ifndef est une directive de préprocesseur qui vérifie si le symbole ACCOUNT_H n'est pas défini.
// Si ce n'est pas le cas, le code entre #ifndef et #endif est inclus dans le fichier. 
// Si le symbole ACCOUNT_H est défini, le code entre #ifndef et #endif est ignoré.

// #define est une directive de préprocesseur qui définit le symbole ACCOUNT_H.
#define MATH_UTILS_H

// Code Section
int addition(int a, int b);
int soustraction(int a, int b);
int multiplication(int a, int b);
double division(int a, int b);


// Fin Code Section

// #endif est une directive de préprocesseur qui indique la fin de la définition du symbole ACCOUNT_H.
#endif